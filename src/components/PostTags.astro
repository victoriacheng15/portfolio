---
interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-2">
	{
		tags.map((tag) => (
			<a
				href={`/tags/${tag}`}
				class="bg-secondary-950 hover:bg-secondary-900 rounded-full px-3 py-1 text-sm duration-300 ease-in-out"
			>
				#{tag}
			</a>
		))
	}
</div>
